<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="sale_tipo_cliente_tree_view" model="ir.ui.view">
        <field name="name">sale.tipo.cliente.tree</field>
        <field name="model">sale.tipo.cliente</field>
        <field name="arch" type="xml">
		<tree string="Tipo de cliente" >
			<field name="name" />
		</tree>
	    </field>
	</record>

    <record id="action_tipo_cliente" model="ir.actions.act_window">
        <field name="name">Tipos de cliente</field>
        <field name="res_model">sale.tipo.cliente</field>
        <field name="view_mode">tree,form</field>
    </record>

    <menuitem action="action_tipo_cliente" id="menu_tipo_cliente" 
    parent="sale.menu_sale_config" name="Tipo de cliente" sequence="2" />

 	<!-- account.account_management_menu  -->

</odoo>